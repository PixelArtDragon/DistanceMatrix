# DistanceMatrix
A single-header file for distance matrix

Useful for situations where memory is less of a concern but fast distance calculations are important.
It uses very little of the STL so it should be very portable. Should work fine with negative weights, though that has yet to be tested.